{% extends 'admin_base.html.twig' %}

{% block title %}Yeni Kategori{% endblock %}

{% block customScript %}
    <script>
        let pageVue = new Vue({
            delimiters: ['${', '}'],
            el: '#page',
            data: {},
            methods: {},
            computed: {},
            watch: {},
            created() {
                let _this = this;
                $(document).ready(function () {
                    $(".select2").select2();
                    let datas=[];
                    $.each(_this.selectedCategory, function (index, value) {
                        datas.push(value.id);
                    });
                    $("#product_category").val(datas).trigger('change');
                })
            }
        });
    </script>
{% endblock %}
{% block body %}
    <div class="col-sm-12" id="pageTitleDiv">
        <div class="col-sm-6">
            <h1>Yeni Kategori</h1>
        </div>
        <div class="col-sm-6">
            <a href="{{ path('category_index') }}" class="btn btn-sm btn-dark" id="createNew">Listeye Dön</a>
        </div>
    </div>

    {{ include('category/_form.html.twig') }}
{% endblock %}
